using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Dialogue : MonoBehaviour
{
    void Start()
    {
        GenericDialogue dialogue = GenericDialogue.Instance();
        dialogue.SetTitle("Hello player!");
        dialogue.SetMessage("Do you want to exchange some gems for 100 gold?");
        dialogue.SetOnAccept("Yes", () => { // define what happens when user clicks Yes:
            if (player.HasGems(10))
            {
                player.RemoveGems(10);
                player.AddGold(100);
            }
            dialogue.Hide();
        });

        dialogue.SetOnDecline("No thanks", () => dialog.Hide());
        dialogue.Show();
    }

    void Update()
    {

    }
}
*/